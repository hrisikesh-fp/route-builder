import { RoutingConfigPanel } from "@/components/routing-config-panel"

/** Dev-only preview — open /dev/routing-config to inspect Routing Config without the map. */
export default function DevRoutingConfigPage() {
  return <RoutingConfigPanel />
}
