# 2026-05-29

- **L1/L2 colors — orange**: capacity warning banners (collapsed route card) and per-product Zone A text now use orange `#fb923c` and background `rgba(251,146,60,0.1)`. Previously amber for banners and purple/indigo for Zone A per-product lines.
- **L3 colors — amber**: stop-runout warnings moved from red to amber `#eab308`. Collapsed route banner now amber instead of red. Red is reserved for product incompatibility (L0) only.
- **Stop warning strip redesign**: red bordered card + red strip replaced by a standalone `#1b1b1b` bg strip below the card in amber. Card itself no longer shows a red border.
- **Stop warning copy updated**: "will run out before this stop" for first-failing products; "already ran out" for downstream stops where product was already negative.
- **Truck section spacing**: capacity message left indent `12px → 36px`; View Truck Details button left indent `8px → 24px` to match Figma spec.
- **Below-truck message mirrors banner**: expanded card now shows "Exceeding/Below Truck Capacity ↑↓ N gal" with arrow and delta right-aligned — matches the collapsed banner exactly.
- **Zone B banner fires without loads**: previously the collapsed banner was hidden if no load orders existed. Now shows for routes where demand exceeds truck capacity even before any load is added.
- **truckMessage**: replaced "This truck can accommodate more orders." with the correct capacity direction copy ("Exceeding/Below Truck Capacity") when L1 fires.
- **suppressL1L2 banner copy fix**: multi-load + L3-passes path was hard-coding "Below Truck Capacity" regardless of actual direction — now uses `l1.status` correctly.
- **MidRouteAddLoadCTA redesign**: replaced plain indigo CTA with two-row component — amber "N deliveries can't be fulfilled" badge (centered, no icon) connected to ⊗ XCircle icon via dashed amber arm; indigo "Add another load order" + "+ Add Load Order" row below, aligned with order cards.
- **Amber dashed connector line**: sequence column turns amber dashed from the ⊗ break point through all subsequent failing stops. Uses CSS repeating-linear-gradient overlay per card.
- **R3 (Jessica Harper) — no seeded load order**: removed the pre-seeded `r3-load` so R3 starts in the correct no-load initial state.
- **R3 — no pre-assigned truck**: H-310 removed from R3's initial state. Truck selection from the dropdown is now step 0, triggering validation on selection.
- **Add Load Order modal — R3 demo orders**: two new Flint Hills entries for the three-step demo — `lo-r3-load1` (Diesel 2,500 + Gas 87 1,500 gal, 05:30 AM) and `lo-r3-load2` (Diesel 500 + Gas 500 gal, 09:15 AM). Load 1 places at hub; Load 2 places between Lost Creek and Barton Creek when added from the mid-route CTA.
- **1/2 counter border removed**: removed the reddish border from the issue navigator pill on the expanded route card banner.
- **Branch**: `iter/workspace-updates`
