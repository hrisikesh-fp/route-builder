# 2026-06-10

## Truck dropdown empty state updated

The "no truck selected" state of the truck dropdown now has the correct visual treatment:

- **"Select Truck"** title now renders in `#a3a3a3` (was `#737373`) — lighter, clearly a placeholder
- **"No truck selected"** subtitle added below the title in `#737373` — gives the user clear feedback on the state
- **Plus icon** (20px) replaces the ChevronDown in the empty state — signals "add" rather than "expand"
- **Helper text** below the card: "Trailers can be added only after adding a Truck" in `#737373` — was missing entirely
- **Copy unified** across the collapsed route card and the dropdown: both now say "No truck selected" (old collapsed card said "No Truck selected yet.")
- Row hover behavior unchanged — transparent by default, `#282828` on hover

---

## Dev handoff doc rewritten

`HANDOFF-truck-dropdown.md` was rewritten from the code — all 8 dropdown states documented with exact JSX snippets, interaction rules, and token values. No Figma references needed.

States covered: empty, truck only, truck + 1 trailer, truck + 2 trailers, truck search open, trailer search open, capacity warning banner, info tooltip.
