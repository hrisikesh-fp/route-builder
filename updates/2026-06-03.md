# 2026-06-03

- **Edit Order — customer card redesign**: the read-only customer/ship-to card at the top of the Edit Order modal now matches Figma — `#282828` bg, 4px radius, no border, customer name and ship-to on one row separated by a grey dot, map pin icon at 16px.
- **Pencil (edit) button on order card hover**: hovering an order card in the workspace now shows a pencil icon before the 3-dot menu. One click opens Edit Order directly — no menu required. Works on both condensed and detailed card views.
- **Settings toggle — Edit Button on Order Card**: new toggle in Settings → Preferences (indigo switch, default ON, persisted to localStorage). When ON, pencil appears in the FAB and Edit is removed from the 3-dot dropdown. When OFF, the reverse — Edit stays only in the 3-dot.
- **Settings modal — fixed height + scroll**: settings modal now has a 720px max-height with the header fixed and content scrolling.
- **Date change warning (create from route)**: when the Create Order modal is opened from a route context and the user picks a different planned date, a confirmation dialog appears — "Change Planned Date? / Changing the planned date to Xth Month YYYY will move this order out from the route." Cancel reverts; "Yes, Proceed Anyway" applies the new date.
- **Empty order warning**: clicking "Create Order" with all quantities at 0 now shows a "Create Order? / Are you sure you want to create an order without adding any asset and product details?" confirmation. Cancel keeps modal open; "Yes, Create" submits.
- **Route Summary — Totals block**: the Route Summary modal now has a `#282828` totals block below the balance table showing Total Load Qty and Total Delivery Qty — grand total in the first column, per-product breakdown in each subsequent column, columns aligned with the table above.
- **Route map dasharray fix**: fixed a console error (`line-dasharray[0] is less than minimum value 0`) caused by sub-millisecond `performance.now()` jitter during the route draw-in animation.
- **Branch**: `iter/workspace-createorder-flow` → merged to `main`
- **Dev handoff**: `HANDOFF-edit-order-flow.md` in repo root with exact file/line references for all net-new pieces.
